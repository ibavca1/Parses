using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace WebParsingFramework.Tests.Unit
{
    [TestClass]
    public class WebCompanyTest
    {
        [TestMethod]
        public void Test_Constructor()
        {
            //// arrange
            //const int id = 10;
            //const string name = "name";
            //var uri = new Uri("http://localhost");

            //// act
            //var company = new WebCompany(id, name, uri);

            //// assert
            //Assert.AreEqual(id, company.Id);
            //Assert.AreEqual(name, company.Name);
            //Assert.AreEqual(uri, company.SiteUri);
        }
    }
}